# RAGSmith – Python dependencies
# All open-source, all free (MIT / Apache 2.0 / BSD)

# ── Web Framework ────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # File uploads via FastAPI
jinja2>=3.1.3                # HTML templates
aiofiles>=23.2.1             # Async static file serving

# ── Embeddings ───────────────────────────────────────────────────────────────
sentence-transformers>=2.7.0  # Apache 2.0 – all-MiniLM-L6-v2, bge-small-en
torch>=2.2.0                  # Required by sentence-transformers (CPU build OK)

# ── Vector Store ─────────────────────────────────────────────────────────────
faiss-cpu>=1.8.0              # MIT – local similarity search, no GPU required

# ── Document Parsing ─────────────────────────────────────────────────────────
pymupdf>=1.24.0               # AGPL – PDF extraction (primary)
pdfminer.six>=20221105        # MIT  – PDF extraction (fallback)
python-docx>=1.1.0            # MIT  – DOCX extraction

# ── Core Python ──────────────────────────────────────────────────────────────
numpy>=1.26.0
pydantic>=2.0.0

# ── LLM (Ollama) ─────────────────────────────────────────────────────────────
# Ollama itself is a separate binary – install from https://ollama.com
# The Python code communicates with Ollama over its local REST API (stdlib urllib)
# No Python Ollama package needed.
